<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <%= form_with(url: sessions_path, class: "login-form") do |form| %>        
        <h2 class="text-center mb-4">Iniciar sesión</h2>

        <% if flash[:danger] %>
          <div class="alert alert-danger">
            <%= flash[:danger] %>
          </div>
        <% end %>

        <div class="form-group">
          <%= form.label :login, class: "font-weight-bold" %>
          <%= form.text_field :login, class: "form-control", placeholder: "Correo electrónico o nombre de usuario", autocapitalize: "off" %>
        </div>
        
        <div class="form-group">
          <%= form.label :password, class: "font-weight-bold" %>
          <%= form.password_field :password, class: "form-control", placeholder: "Contraseña" %>
        </div>
      
        <div class="form-group">
          <%= form.submit "Iniciar sesión", class: "btn btn-primary btn-block" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
